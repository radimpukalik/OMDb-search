import "../styles/components/NoFavoritesMessage.css";

const NoFavoritesMessage = () => {
  return (
    <div className="no-favorites-message">
      <h2>Unfortunately, you have no favorites in your list...</h2>
      <p>Browse through the movies and add some to your favorites!</p>
    </div>
  );
};

export default NoFavoritesMessage;
